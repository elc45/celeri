`*_segment.csv` files define the block model geometry.  The columns are:

`name`:

`lon1`:

`lat1`:

`lon2`:

`lat2`:

`dip`:

`res`:

`other3`:

`other6`:

`other7`:

`other8`:

`other9`:

`other10`:

`other11`:

`create_ribbon_mesh`:

`locking_depth`:

`locking_depth_sig`:

`locking_depth_flag`:

`dip_sig`:

`dip_flag`:

`ss_rate`:

`ss_rate_sig`:

`ss_rate_flag`:

`ds_rate`:

`ds_rate_sig`:

`ds_rate_flag`:

`ts_rate`:

`ts_rate_sig`:

`ts_rate_flag`:

`resolution_override`:

`resolution_other`:

`patch_file_name`:

`patch_flag`:

`patch_slip_file`:

`patch_slip_flag`:

`ss_rate_bound_flag`:

`ss_rate_bound_min`:

`ss_rate_bound_max`:

`ds_rate_bound_flag`:

`ds_rate_bound_min`:

`ds_rate_bound_max`:

`ts_rate_bound_flag`:

`ts_rate_bound_min`:

`ts_rate_bound_max`:


